# 1. Tạo thư mục và copy file vào
mkdir ai-finance-cards && cd ai-finance-cards
# (đặt index.html và README.md vào đây)

# 2. Khởi tạo git và push
git init
git add .
git commit -m "init: ai finance cards"
git branch -M main
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin main
